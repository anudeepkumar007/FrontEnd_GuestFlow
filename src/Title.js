import { Typography } from "@mui/material";
import React from "react";

const Title = () => {
    return (
        
        <Typography variant="h5"
        sx={{ mt: 2, ml: 2 ,
          fontWeight: "bold",
          color: "black",
        }}>
            GuestFlow
          </Typography>
          

    );
};

export default Title;